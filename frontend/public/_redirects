# Netlify / some static hosts: SPA fallback (copied to dist with Angular assets).
# Render Static Sites do NOT read this file — add a Dashboard rewrite (see deploy/render.env.example).
/*    /index.html   200
